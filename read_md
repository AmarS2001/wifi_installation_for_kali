This shell script helps you to obtain wifi adapters and connect to wifi connection, instead of ethernet connection.
So, download the shell script and run "./amar_script.sh"
wait for few seconds and check it by running "ifconfig"
